import { AgarDirective } from './agar.directive';

describe('AgarDirective', () => {
  it('should create an instance', () => {
    const directive = new AgarDirective();
    expect(directive).toBeTruthy();
  });
});
